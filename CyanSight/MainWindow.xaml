<ui:FluentWindow x:Class="CyanSight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:md="clr-namespace:MdXaml;assembly=MdXaml"
        xmlns:vm="clr-namespace:CyanSight.ViewModels"
        mc:Ignorable="d"
        Title="CyanSight" 
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Background="Transparent"
        d:DesignHeight="700" 
        d:DesignWidth="1000">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="FluentTabItemStyle" 
               TargetType="TabItem">
            <Setter Property="FontSize" 
                    Value="14"/>
            <Setter Property="Background" 
                    Value="Transparent"/>
            <Setter Property="Foreground" 
                    Value="{DynamicResource TextFillColorSecondaryBrush}"/>
            <Setter Property="BorderThickness" 
                    Value="0"/>
            <Setter Property="Padding" 
                    Value="16,8"/>
            <Setter Property="Margin" 
                    Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                Margin="0" 
                                SnapsToDevicePixels="True">
                            <ContentPresenter x:Name="ContentSite"
                                      VerticalAlignment="Center"
                                      HorizontalAlignment="Center"
                                      ContentSource="Header"
                                      Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" 
                                     Value="True">
                                <Setter TargetName="Border" 
                                        Property="Background" 
                                        Value="{DynamicResource ControlFillColorDefaultBrush}"/>
                                <Setter Property="Foreground" 
                                        Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                <Setter Property="FontWeight" 
                                        Value="Medium"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" 
                                     Value="True">
                                <Setter TargetName="Border" 
                                        Property="Background" 
                                        Value="{DynamicResource SubtleFillColorSecondaryBrush}"/>
                                <Setter Property="Foreground" 
                                        Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="CyanSight" CloseWindowByDoubleClickOnIcon="True" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="12,0,0,12">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <ui:TextBox Grid.Row="0" 
                            PlaceholderText="搜索优化项..." 
                            Icon="{ui:SymbolIcon Search24}" 
                            Margin="0,0,12,12"/>

                <TabControl Grid.Row="1" 
                            Background="Transparent" 
                            BorderThickness="0" 
                            Padding="0">

                    <TabItem Header="🚀 核心优化" 
                             Style="{StaticResource FluentTabItemStyle}">
                        <Grid Margin="-5,5,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Rectangle Grid.Row="0" 
                       Height="1" 
                       Fill="{DynamicResource SurfaceStrokeColorDefaultBrush}" 
                       Opacity="0.4"
                       Margin="5,0,0,8"/>

                            <ListView Grid.Row="1"
                      ItemsSource="{Binding CoreItems}"
                      Background="Transparent"
                      BorderThickness="0"
                      SelectedItem="{Binding SelectedItem}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsSelected}" 
                                                      IsEnabled="{Binding IsEnabled}" 
                                                      Grid.Column="0" Margin="0,0,12,0"/>
                                            <StackPanel Grid.Column="1" 
                                                        VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}" 
                                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" 
                                                           FontWeight="Medium" 
                                                           TextTrimming="CharacterEllipsis"/>
                                                <TextBlock Text="{Binding Category}" 
                                                           FontSize="10" 
                                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" 
                                                           Margin="0,2,0,0"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </TabItem>

                    <TabItem Header="🏛️ 怀旧" Style="{StaticResource FluentTabItemStyle}">
                        <Grid Margin="0,5,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Rectangle Grid.Row="0" 
                                       Height="1" 
                                       Fill="{DynamicResource SurfaceStrokeColorDefaultBrush}" 
                                       Opacity="0.4" 
                                       Margin="0,0,0,8"/>

                            <Border Grid.Row="1" 
                                    Background="#15FF0000" 
                                    BorderBrush="#30FF0000" 
                                    BorderThickness="1" 
                                    CornerRadius="4" 
                                    Margin="0,0,12,10" 
                                    Padding="10">
                                <StackPanel Orientation="Horizontal">
                                    <ui:SymbolIcon Symbol="Warning24" 
                                                   Foreground="OrangeRed" 
                                                   Margin="0,0,10,0"/>
                                    <TextBlock Text="仅供展示过时项目，建议仅查阅。" 
                                               Foreground="OrangeRed" 
                                               FontSize="11" 
                                               TextWrapping="Wrap" 
                                               VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>

                            <ListView Grid.Row="2" 
                      ItemsSource="{Binding LegacyItems}" 
                      Background="Transparent" 
                      BorderThickness="0"
                      SelectedItem="{Binding SelectedItem}">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,6" Opacity="0.8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <ui:SymbolIcon Symbol="History24" 
                                                           Grid.Column="0" 
                                                           Margin="0,0,12,0" 
                                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
                                            <StackPanel Grid.Column="1" 
                                                        VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Title}" 
                                                           TextTrimming="CharacterEllipsis" 
                                                           Foreground="Gray" 
                                                           TextDecorations="Strikethrough"/>
                                                <TextBlock Text="已过时" 
                                                           FontSize="10" 
                                                           Margin="0,2,0,0" 
                                                           Foreground="Gray"/>
                                            </StackPanel>
                                        </Grid>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>

            <Rectangle Grid.Column="0" 
                       HorizontalAlignment="Right" 
                       Width="1" Fill="{DynamicResource SurfaceStrokeColorDefaultBrush}" 
                       Margin="0,12"/>

            <ui:Card Grid.Column="1" 
                     VerticalAlignment="Stretch" 
                     Padding="0"
                     Margin="12,12,12,12" 
                     Background="Transparent"
                     BorderThickness="0">
                <Grid>
                    <TextBlock Text="请在左侧选择一个项目查看详情" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Visibility="{Binding SelectedItem, Converter={StaticResource NullToVisibleConverter}}"
                               Foreground="Gray"/>

                    <Grid Visibility="{Binding SelectedItem, Converter={StaticResource NullToCollapsedConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0"
                                Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"                           
                                BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}"                              
                                BorderThickness="0,0,0,1" 
                                Padding="32,24,32,24">
                            <StackPanel>
                                <TextBlock Text="{Binding SelectedItem.Title}" 
                                           FontSize="24" 
                                           FontWeight="SemiBold"/>

                                <ui:ToggleSwitch IsChecked="{Binding SelectedItem.IsSelected, Mode=TwoWay}" 
                                                 IsEnabled="{Binding SelectedItem.IsEnabled}" 
                                                 Margin="0,16,0,0">
                                    <ui:ToggleSwitch.Style>
                                        <Style TargetType="ui:ToggleSwitch" BasedOn="{StaticResource {x:Type ui:ToggleSwitch}}">
                                            <Setter Property="Content" Value="启用此优化 (将修改注册表)"/>
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding SelectedItem.IsEnabled}" Value="False"/>
                                                        <Condition Binding="{Binding SelectedItem.IsSelected}" Value="True"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Content" Value="⚠️ 检测状态：系统中已启用 (此项已过时)"/>
                                                    <Setter Property="Opacity" Value="1.0"/>
                                                </MultiDataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding SelectedItem.IsEnabled}" Value="False"/>
                                                        <Condition Binding="{Binding SelectedItem.IsSelected}" Value="False"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Content" Value="🛡️ 检测状态：未检测到 (系统纯净)"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ui:ToggleSwitch.Style>
                                </ui:ToggleSwitch>
                            </StackPanel>
                        </Border>

                        <md:MarkdownScrollViewer Grid.Row="1" 
                                                 Markdown="{Binding SelectedItem.Description}" 
                                                 FlowDirection="LeftToRight"
                                                 HorizontalScrollBarVisibility="Disabled"
                                                 VerticalScrollBarVisibility="Auto"
                                                 Margin="0">

                            <md:MarkdownScrollViewer.MarkdownStyle>
                                <Style TargetType="FlowDocument">
                                    <Setter Property="FontFamily" Value="Microsoft YaHei UI, Segoe UI"/>
                                    <Setter Property="FontSize" Value="14"/>
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}"/>
                                    <Setter Property="TextAlignment" Value="Left"/>
                                    <Setter Property="PagePadding" Value="32"/>
                                    <Setter Property="LineHeight" Value="24"/>
                                </Style>
                            </md:MarkdownScrollViewer.MarkdownStyle>

                        </md:MarkdownScrollViewer>
                    </Grid>
                </Grid>
            </ui:Card>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource SolidBackgroundFillColorSecondaryBrush}" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource SurfaceStrokeColorDefaultBrush}" Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <ui:DropDownButton Icon="{ui:SymbolIcon ArrowUpload24}" 
                       Content="导入配置" 
                       Appearance="Secondary" 
                       Margin="0,0,8,0">
                        <ui:DropDownButton.Flyout>
                            <ContextMenu>
                                <MenuItem Header="浏览本地文件..." Command="{Binding ImportProfileCommand}">
                                    <MenuItem.Icon>
                                        <ui:SymbolIcon Symbol="FolderOpen24"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <Separator/>
                                <MenuItem Header="系统预设配置" IsEnabled="False" Foreground="Gray" FontSize="11"/>

                                <MenuItem Header="全选" 
                          Command="{Binding ApplyPresetCommand}" 
                          CommandParameter="AllSelect">
                                    <MenuItem.Icon>
                                        <ui:SymbolIcon Symbol="SelectAllOn24"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <MenuItem Header="清除 (全不选)" 
                          Command="{Binding ApplyPresetCommand}" 
                          CommandParameter="AllNotSelect">
                                    <MenuItem.Icon>
                                        <ui:SymbolIcon Symbol="CheckboxUnchecked24"/>
                                    </MenuItem.Icon>
                                </MenuItem>

                                <Separator/>

                                <MenuItem Header="Surface Go 2" 
                          Command="{Binding ApplyPresetCommand}" 
                          CommandParameter="surfacego2"/>

                                <MenuItem Header="重置为默认值" 
                          Command="{Binding ApplyPresetCommand}" 
                          CommandParameter="Reset">
                                    <MenuItem.Icon>
                                        <ui:SymbolIcon Symbol="ArrowCounterclockwise24"/>
                                    </MenuItem.Icon>
                                </MenuItem>
                            </ContextMenu>
                        </ui:DropDownButton.Flyout>
                    </ui:DropDownButton>

                    <ui:Button Icon="{ui:SymbolIcon ArrowDownload24}" 
               Content="导出配置" 
               Command="{Binding ExportProfileCommand}" 
               Appearance="Secondary" 
               Margin="0,0,8,0"/>

                    <ui:Button Icon="{ui:SymbolIcon ArrowCounterclockwise24}" 
               Content="恢复默认" 
               Command="{Binding RestoreDefaultCommand}" 
               Appearance="Secondary"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="配置已就绪，等待应用" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorSecondaryBrush}" FontSize="12" Margin="0,0,12,0"/>
                    <ui:Button Icon="{ui:SymbolIcon Checkmark24}" Content="立即应用更改" Command="{Binding ApplyChangesCommand}" Appearance="Primary" Width="140"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>